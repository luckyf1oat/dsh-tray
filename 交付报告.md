# dsh-tray 交付报告

> 交付日期：2026-08-17 ｜ 基于 [KAIbsb/dsh-tray](https://github.com/KAIbsb/dsh-tray) v1.1.3 源码重新审查、修复、测试后构建
> 对比项目：[qing3a/dsh-tray](https://github.com/qing3a/dsh-tray)（未选为交付基座，见下）

---

## 一、项目选型对比结论

| 维度 | KAIbsb/dsh-tray（✅ 选中） | qing3a/dsh-tray |
| --- | --- | --- |
| 定位 | **独立 exe 托盘管家**：完整生命周期管理（启动/重启/停止/崩溃自动拉起/开机自启/自动更新） | DSH 插件式轻量托盘（托盘图标+菜单+气泡通知） |
| 形态 | 单文件 dsh-tray.exe（.NET Framework 4.8，零依赖） | TypeScript 插件，随 DSH 进程加载（`pnpm dsh plugin add`） |
| 语言 | C# / WinForms | TS / cordis 插件 |
| 功能覆盖 | 生命周期 + 状态图标 + 主题 + 提权 + 更新 | 仅托盘常驻与通知 |
| 维护性 | 自带 headless 自检（--smoke/--menu-test/--ui-preview）+ CI | 简单但功能少 |
| 风险 | 无签名 exe（SmartScreen 提示） | 依赖 trayicon@1.2.2 第三方 exe 宿主 |

**选型理由**：KAIbsb 版是独立成品、功能完整、有自检设施可做真实验证；qing3a 版是插件，交付形态不满足「独立托盘管家 + 桌面快捷方式」需求。

---

## 二、代码审查结论（3200+ 行，全部人工逐文件审查）

### 审查发现并修复的问题

| # | 严重度 | 问题 | 修复 |
| --- | --- | --- | --- |
| 1 | **中（真实竞态）** | `StartCore()` 无状态守卫：用户「停止」与自动启动并发时，spawn 出的 harness 可能残留运行（停止后端口仍被占用、状态却显示 Stopped） | spawn 前在 `stateLock` 内复核 `state == Starting`，否则中止 spawn（`DshProcess.cs`） |
| 2 | 低 | `--smoke` 报告 `port3080` 缺空格 | `Program.cs` 改为 `port 3080` |
| 3 | 低（可测试性） | `UpdateCheck` 版本比较/JSON 提取辅助方法为 private，无法单测 | 改为 public（纯函数，无副作用） |

### 审查确认的优质设计（未改动）

- **fail-closed 身份检查**：停止/提权杀进程前，必须确认目标是 node 且命令行含 dsh 特征（`@deepseek-ai`/`bin.js`/`\dsh\`），拒绝误杀任意进程
- **提权 kill 一次性 nonce + token 校验**：UAC 提权助手带随机 nonce + dsh entry 双重验证，防伪造调用
- **adopt 防双开**：端口已被 dsh 服务时自动收养而非重复 spawn
- **cmd 变量注入防护**：启动命令用环境变量占位，路径中的 `& | ^ ( )` 无法注入
- **崩溃自动重启冷却**：10s 启动冷却 + 30s 重试冷却，防死循环
- **单实例互斥** + 残留更新文件自动清理

---

## 三、测试结果

### 1. 自带 headless 自检（修复后回归）
- `--smoke`：✅ SMOKE OK（node ✓ / chrome ✓ / 图标资源 ✓ / 行校验 ✓）
- `--menu-test`：✅ menu-test OK（items=6, defs=9）
- `--ui-preview`：✅ 6 张设置窗渲染图（亮/暗 × 100%/125%/150% DPI）

### 2. 集成测试（42 项全绿，独立测试台驱动真实 DshProcess 状态机）
```
=== RESULT: 42 passed, 0 failed ===
```
覆盖：初始状态 / 启动拉起端口 / 幂等重复启动 / 崩溃→Stopped / 自动重启拉回 /
停止释放端口 / 重启（Stopped 与 Running 两态）/ 孤儿 harness 收养不双开 /
收养后停止 / 端口探测正确性 / 启动命令引号 / ini 读写往返 / 版本比较 / JSON 提取 /
**并发 start/stop 竞态 5 轮无泄漏**（针对修复 #1 的回归）

### 3. GUI 真机冒烟（托盘 exe 实际运行）
- 托盘进程启动 ✅
- 自动拉起 harness（`StartDsh: launched` + `WaitForPortUpAsync: up=True`）✅
- 更新检查联网正常（up to date v1.1.3）✅
- 退出后清理干净、真实 3080 harness 不受影响 ✅

### 4. dev checkout 适配验证
- 交付目录内 `entry\dsh\bin.js` 包装器可正常拉起真实 dev harness（`node --import tsx/esm apps/cli/src/bin.ts "web"`）✅
- 信号转发（SIGTERM→子进程）使托盘 Stop/Restart 的 taskkill /T 能杀到真实 harness ✅

---

## 四、交付物清单

```
D:\dsworkspace\dsh-tray-delivery\
├── dsh-tray.exe            # 成品（含修复，121,856 字节）
├── dsh-tray.exe.sha256     # SHA-256: dedda07d...
├── dshtray.ini             # 配置（默认 3080 端口）
├── entry\dsh\bin.js        # dev checkout 适配入口（标准安装可删）
└── 交付报告.md             # 本文件
```

**桌面快捷方式**：`C:\Users\Administrator\Desktop\dsh-tray.lnk`（指向 dsh-tray.exe）

---

## 五、使用说明

1. 双击桌面 `dsh-tray` 快捷方式 → 托盘出现鲸鱼图标 → 自动启动 DSH harness（无终端窗口）
2. 右键托盘：打开窗口 / 启动 / 重启 / 停止 / 设置… / 退出
3. 左键单击托盘：未运行则启动+开窗，运行中直接开窗
4. 状态图标：蓝色鲸鱼=运行中；黑白鲸=已停止
5. 配置在 exe 同目录 `dshtray.ini`，改端口改 `url` 即可
6. 「退出」只退托盘，harness 保持运行；要停 harness 用「停止」

### 本机（dev checkout）已配置开箱即用
`dshtray.ini` 已填 `dshentry=D:\dsworkspace\dsh-tray-delivery\entry\dsh\bin.js`，
托盘可正常启动/停止/重启 `D:\deepseek-harness` 的 dev harness（3080 端口）。

### 标准 npm 全局安装用户
把 `dshtray.ini` 里 `dshentry` 与 `dshworkdir` 两行留空即可，托盘自动检测 `@deepseek-ai/dsh`；
此时 `entry\dsh\bin.js` 可删除。

---

## 六、已知限制

- exe 未签名，首次运行 SmartScreen 提示「未知发布者」→ 更多信息 → 仍要运行
- 自动更新功能面向 KAIbsb 上游仓库（Release 页面），若从本交付继续使用需注意上游版本
- 本机 dev harness 由 wrapper 适配；标准安装环境无此问题
